import { FilterAirportPipe } from './filter-airport.pipe';

describe('FilterAirportPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAirportPipe();
    expect(pipe).toBeTruthy();
  });
});
